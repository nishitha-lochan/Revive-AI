# Optional: Enable live GPT-4o LLM reasoning (leave blank for heuristic mode)
OPENAI_API_KEY=

# Optional: GitHub Personal Access Token (for private repos / rate limits)
GITHUB_TOKEN=

# Optional: PostgreSQL for production (uses SQLite by default)
DATABASE_URL=sqlite:///./revive_ai.db
